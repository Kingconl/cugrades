# Production backend URL for the frontend.
# Leave blank if your frontend and backend are served from the same origin.
VITE_API_BASE=

# Optional: local dev proxy target used by Vite.
VITE_DEV_API_PROXY_TARGET=http://localhost:8080
